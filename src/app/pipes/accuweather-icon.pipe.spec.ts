import { AccuweatherIconPipe } from './accuweather-icon.pipe';

describe('AccuweatherIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AccuweatherIconPipe();
    expect(pipe).toBeTruthy();
  });
});
